const base="https://cascadelogistics.vercel.app/api/v1";
export default function QuickstartPage(){const curl=`curl ${base}/me \\\n+  -H "Authorization: Bearer csl_test_YOUR_KEY"`;return <main className="mx-auto max-w-4xl px-5 py-14"><h1 className="text-4xl font-black">Test-mode quick-start</h1><ol className="mt-8 space-y-6 text-slate-300"><li><b>1. Get access.</b> Your Cascade integration owner supplies an organization slug and portal member account.</li><li><b>2. Create a sandbox application.</b> Choose only the scopes your backend needs.</li><li><b>3. Issue a test key.</b> Copy it immediately; only its prefix is retained for display.</li><li><b>4. Verify identity.</b><pre className="mt-3 overflow-x-auto rounded-xl bg-black p-4 text-sm text-cyan-200">{curl}</pre></li><li><b>5. Upload files, then create the shipment.</b> Call <code>POST /uploads</code> for each file, upload to the returned private target, then pass every upload ID to <code>POST /shipments</code> with a unique <code>Idempotency-Key</code>.</li><li><b>6. Configure webhooks.</b> Save the one-time signing secret and verify the timestamp plus HMAC signature against the exact raw body.</li></ol><p className="mt-10 rounded-xl border border-amber-300/20 bg-amber-300/10 p-4 text-amber-100">Test and live records never mix. Never place an API key in browser code or a mobile application.</p></main>}
